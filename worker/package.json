{
  "name": "slimora-ai-proxy",
  "description": "Cloudflare Worker that fronts Groq/OpenRouter so provider keys never ship in the app.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260808.1",
    "typescript": "~5.9.2",
    "wrangler": "^4.120.0"
  }
}
